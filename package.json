{
  "name": "calorie-app",
  "private": true,
  "description": "The app itself is a single static HTML file and needs nothing built or installed. The one serverless function (netlify/functions/dining-menu.js) uses only Node's built-in fetch, so there are no production dependencies. puppeteer-core is a DEV dependency only: the test suites in Projects/Calorie App/tests drive a real Chrome through it. It is never shipped.",
  "dependencies": {},
  "devDependencies": {
    "puppeteer-core": "^25.8.0"
  }
}
